
fn main() {
    let n1 = 27;
    let n2 = 72;
    let prod = multiply(n1, n2);
    println!("Product of {} and {} is {}", n1, n2, prod);
}

fn multiply(a1: i32, a2: i32) -> i32 {
    let p = a1 * a2;
    p
}
